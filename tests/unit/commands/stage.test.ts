import stage from "../../../src/commands/stage";

describe("bit stage [files...]", () => {
  it("should stage all files given files/directories", () => {});

  it("should not stage files already staged", () => {});
});
